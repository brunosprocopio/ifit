<%= form_for(@alimento_preco) do |f| %>
  <% if @alimento_preco.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@alimento_preco.errors.count, "error") %> prohibited this alimento_preco from being saved:</h2>

      <ul>
      <% @alimento_preco.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :preco %><br>
    <%= f.number_field :preco %>
  </div>
  <div class="field">
    <%= f.label :prato_montado_id %><br>
    <%= f.text_field :prato_montado_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
