require 'rails_helper'

RSpec.describe "PratoMontados", type: :request do
  describe "GET /prato_montados" do
    xit "works! (now write some real specs)" do
      get prato_montados_path
      expect(response).to have_http_status(200)
    end
  end
end
