require 'rails_helper'

RSpec.describe "PratoProntos", type: :request do
  describe "GET /prato_prontos" do
    xit "works! (now write some real specs)" do
      get prato_prontos_path
      expect(response).to have_http_status(200)
    end
  end
end
