require 'rails_helper'

RSpec.describe "PedidosHasPratos", type: :request do
  describe "GET /pedidos_has_pratos" do
    xit "works! (now write some real specs)" do
      get pedidos_has_pratos_path
      expect(response).to have_http_status(200)
    end
  end
end
